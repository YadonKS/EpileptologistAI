# Copy this file to .env and replace the placeholder with your Supabase/DB connection string.
# Do NOT commit a real .env with secrets into source control.

DATABASE_URL=postgresql://DB_USER:DB_PASS@DB_HOST:5432/DB_NAME

# Example with SSL mode (if required by provider):
# DATABASE_URL=postgresql://DB_USER:DB_PASS@DB_HOST:5432/DB_NAME?sslmode=require
